# Example environment variables for Vercel deployment
# Copy these to your Vercel project settings

# Your ngrok URL (get this by running: ngrok http 5000)
BACKEND_API_URL=https://your-ngrok-url.ngrok.io

# Dashboard password (change this to something secure)
NEXT_PUBLIC_DASHBOARD_PASSWORD=hpmrei2025

# Optional: Restrict access to specific IPs (comma-separated)
# Get your IP from: https://whatismyipaddress.com
NEXT_PUBLIC_ALLOWED_IPS=

# Optional: For production, you might want to add:
# NODE_ENV=production
